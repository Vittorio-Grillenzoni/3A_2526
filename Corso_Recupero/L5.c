#include <stdio.h>
int main(){
    int n1 = 0;
    int n2 = 0;
    int cnt;
    do{
        scanf("%d", &n1);
    }while(n1 <= 20);
    do{
        scanf("%d", &n2);
    }while(n2 <= 20);
    for(cnt = 1; cnt<= n1; )
    return 0;
}